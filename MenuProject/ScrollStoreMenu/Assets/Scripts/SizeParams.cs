using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public struct SizeParams
{
    public float x, y;
    public SizeParams(float x, float y)
    {
        this.x = x;
        this.y = y;
    }
}
